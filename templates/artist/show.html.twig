{% extends 'base.html.twig' %}

{% block title %}Fiche d'un artiste{% endblock %}

{% block body %}
    <h1>{{ artist.firstname }} {{ artist.lastname }}</h1>

    <h2>Liste des types</h2>
    <ul>
        {% for artistType in artist.types  %}
            <li>{{ artistType.type.type }}</li>
        {% endfor %}
    </ul>
    <div><a href="{{ path('artist_edit' , {'id': artist.id}) }}"> Modifier</a></div>

    <nav><a href="{{ path('artist_index') }}">Retour à l'index</a></nav>
{% endblock %}


